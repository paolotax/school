upstream unicorn {
  server unix:/tmp/unicorn.<%= application %>.sock fail_timeout=0;
}

server {
  listen 3000;
  
  root /home/deployer/apps/gino;
  index index.html index.htm;
  
  location / {
    try_files $uri $uri/ /index.html;
  }

}
